<%= form_for @product do |f| %>
	Name
	<%= f.text_field :name %>
	Display Name
	<%= f.text_field :display_name %>
	<%= f.collection_select :category_id, Category.all, :id, :name %>
	<%= f.submit "Create" %>
<% end %>